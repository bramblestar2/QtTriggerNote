#pragma once

#include <QMainWindow>
#include <QWidget>
#include <QMenuBar>
#include <QAction>

#include "core/QtApp.h"

class MainWindow : public QMainWindow {
    Q_OBJECT
public:
    MainWindow(QWidget *parent = nullptr);

private slots:
private:

};